PORT=3000
DATA_DIR=./data

# Segurança do painel admin (obrigatório)
# Gere com: openssl rand -hex 32
ADMIN_TOKEN=
CREDENTIALS_ENCRYPTION_KEY=

# Tenant padrão (uma instância = uma empresa inicial)
DEFAULT_TENANT_SLUG=crescere
DEFAULT_TENANT_NAME=Crescere

# A chave OpenAI NÃO fica no .env — cadastre por empresa em /admin
